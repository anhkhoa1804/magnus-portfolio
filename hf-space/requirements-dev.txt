# requirements-dev.txt -- Fast local installation for testing
# Skips heavy ML libs (prophet, torch, ultralytics) which are optional.
# Features that WORK: Chat (Groq), IELTS Writing/Speaking, German News, Health
# Features that gracefully fail: Price Prediction, Object Detection, Doodle (heuristic fallback)

flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
Pillow>=10.0.0
groq>=0.11.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pandas>=2.1.0
numpy>=1.24.0,<2.0.0
